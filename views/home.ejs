<html>
  <head>
    <title>Home</title>
    <link rel="stylesheet" href="stylesheets/styles.css">
  </head>
  <body>
    <h1> Welcome to Repo Creator </h1>
    <form action="/auth/github/login" method="get">
      <button type="submit">Login With Github</button>
    </form>


    <form action="/auth/profile" method="get">
      <button type="submit">Profile</button>
    </form>


    <form action="/github/createrepository" method="post">
      <button type="submit">REPO+</button>
    </form>

    <!--action="/auth/refresh" method="get"
    <form>
      <button id="myButton" type="submit">Refresh</button>
      <script src="https://cdn.jsdelivr.net/npm/js-cookie@2/src/js.cookie.min.js"></script>
      <script>
        const button = document.getElementById('myButton');
        button.addEventListener('click', function() {
          // Fetch the cookies
          const cookies = getCookies();
          console.log(cookies); // [{ name: 'cookie1', value: 'value1' }, { name: 'cookie2', value: 'value2' }, ...]
  
          // Set the cookie in the Authorization header
          const cookie = getCookie('jwtr');
          const headers = {
            'Authorization': 'Bearer ' + cookie
          };
  
          // Send a GET request with the headers
          fetch('/auth/refresh', {
            method: 'GET',
            headers: headers
          })
          .then(response => response.json())
          .then(data => console.log(data))
          .catch(error => console.error(error));
        });
      </script>
    </form>-->

    <form action="logout" method="get">
      <button type="submit">Logout</button>
    </form>


  </body>
</html>